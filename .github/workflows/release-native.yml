name: Release MAUI Designer

on:
  push:
    tags:
      - 'native-v*'

jobs:
  publish:
    name: Publish Windows app
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Require a commit from main
        shell: pwsh
        run: |
          git fetch origin main
          git merge-base --is-ancestor $env:GITHUB_SHA origin/main
          if ($LASTEXITCODE -ne 0) {
            throw 'Native releases must be tagged from a commit already merged into main.'
          }

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Install MAUI Windows workload
        run: dotnet workload install maui-windows

      - name: Publish unpackaged
        run: dotnet publish maui-designer-native/MAUIDesigner.Fresh.App/MAUIDesigner.Fresh.App.csproj -c Release -r win-x64 -p:PublishReadyToRun=false

      - name: Stage zip
        shell: pwsh
        run: |
          $publish = Get-ChildItem -Recurse -Directory -Filter publish |
            Where-Object { $_.FullName -match 'MAUIDesigner\.Fresh\.App.+net10\.0-windows10\.0\.19041\.0.+win-x64' } |
            Select-Object -First 1
          if (-not $publish) { throw 'publish folder not found' }
          New-Item -ItemType Directory -Force dist | Out-Null
          Compress-Archive -Path (Join-Path $publish.FullName '*') -DestinationPath dist/MAUIDesigner-win-x64.zip -Force

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: MAUI Designer ${{ github.ref_name }}
          prerelease: true
          generate_release_notes: true
          files: dist/MAUIDesigner-win-x64.zip
